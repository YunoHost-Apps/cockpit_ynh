Dans l'application cockpit, un utilisateur sera restreint à un accès limmité si il n'est pas membre du groupe sudo.
Pour ajouter l'utilisateur `myuser` au groupe sudo, utilisez la commande suivante : `sudo usermod -aG sudo myuser`
